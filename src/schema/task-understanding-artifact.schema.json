{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AutoLoop task-understanding artifact — runner-constructed, binds payload to authority",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "source_card_id",
    "authority_digest",
    "payload",
    "contract_digest",
    "designer_provenance_digest",
    "created_at"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "autoloop.task-understanding-artifact/v1"
    },
    "source_card_id": { "type": "string", "minLength": 1, "maxLength": 128 },
    "authority_digest": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "payload": {
      "description": "The validated Designer payload, unmodified. Shape defined by task-understanding-payload.schema.json."
    },
    "contract_digest": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "Runner-computed. Covers authority_digest + semantic payload fields only — excludes timestamps, model name, invocation ID, prompt version, storage path."
    },
    "designer_provenance_digest": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "Runner-computed after this artifact exists. Never depends on challenge results."
    },
    "created_at": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?Z$",
      "description": "Excluded from contract_digest."
    }
  }
}
