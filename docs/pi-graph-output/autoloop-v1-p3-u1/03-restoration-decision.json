{
  "schema": "autoloop.p3-u1.restoration-decision/v1",
  "card_id": "AUTOLOOP-V1-P3-U1-PI-EXECUTION-SEAM-RESTORE-1",
  "DECISION": "RESTORE",
  "WHY": "The adapter's JSONL RPC contract is STILL the officially supported protocol generation of @earendil-works/pi-coding-agent 0.84.2 (--mode rpc exists; all adapter flags exist verbatim; agent_settled / get_last_assistant_text / message_end shapes verified on the real wire). The blocker was purely an installation/environment gap: the `pi` binary (pi-coding-agent CLI) was never installed, while the repo only pins the pi-ai LIBRARY (which has no RPC server). Per card C1: restore/pin the correct Pi executable — no adapter rewrite (C2) or alternate-surface rebinding (C3) needed.",
  "MINIMUM_CHANGE": {
    "environment": [
      "npm install -g @earendil-works/pi-coding-agent@0.84.2  (restores `pi` on PATH; version pinned as installed)",
      "provision deepseek api_key credential into pi's own auth store ~/.pi/agent/auth.json (shape {type:'api_key', key}; pi's login() produces this exact shape) so the adapter's restricted child env can authenticate; verified `pi auth check --provider deepseek --model deepseek-v4-flash` -> ready with env cleared to the adapter allowlist"
    ],
    "repository": [
      "scripts/pi-rpc-usage-probe.mjs — bounded usage-source reachability probe (self-guarded behind ALLOW_REAL_PI_USAGE_PROBE), reuses the adapter's exact spawn args + env allowlist, records the raw JSONL stream and extracts authoritative usage. This is the card-allowed 'real-smoke test' + evidence tool; it does not touch adapter/governance/telemetry code."
    ]
  },
  "non_goals_respected": [
    "no token telemetry wiring (remains original P3 phase)",
    "no node.usage / observer / aggregate / budget changes",
    "no tokenizer estimates",
    "no interactive CLI scraping",
    "no fake/deterministic substitute used as the PASS proof (real DeepSeek invocation, real protocol events)",
    "no architecture redesign"
  ],
  "environment_vs_repository": {
    "ENVIRONMENT_FIX": "pi binary install + credential provisioning (machine setup)",
    "REPOSITORY_FIX": "only the bounded evidence/probe tool; no production source changed"
  }
}
