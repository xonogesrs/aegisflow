{
  "schema": "autoloop.p3-u1.executor-reality-map/v1",
  "card_id": "AUTOLOOP-V1-P3-U1-PI-EXECUTION-SEAM-RESTORE-1",
  "generated_at": "2026-08-18T21:00:00+08:00",
  "executor_reality": {
    "EXPECTED_EXECUTOR": "pi (the @earendil-works/pi-coding-agent CLI, bin name `pi`)",
    "ACTUAL_AVAILABLE_EXECUTOR": "/Users/zhengfengqing/.npm-global/bin/pi",
    "VERSION": "0.84.2",
    "INSTALL_LOCATION": "npm global prefix /Users/zhengfengqing/.npm-global (package @earendil-works/pi-coding-agent@0.84.2)",
    "LAUNCH_METHOD": [
      "adapter spawn via PATH: src/adapter/pi-rpc-adapter.mjs piExecutable='pi' (default)",
      "launcher: scripts/pi-autoloop.sh -> exec pi --approve (npm bin `pia` symlink)",
      "bare `pi` from any cwd (VCA1 P1 route, now executable again)"
    ],
    "STATUS": "RESTORED (was missing; installed 2026-08-18)"
  },
  "recovery_trace": {
    "root_cause": "binary/package installation missing — the pi-coding-agent CLI was not installed on PATH; the repo's package.json only pins the @earendil-works/pi-ai LIBRARY (0.83.0), which does NOT ship the `pi` bin (its bin is `pi-ai` -> dist/cli.js, an auth-login helper only)",
    "identity_evidence": [
      "scripts/pi-autoloop.sh line 29: `exec pi --approve \"$@\"` (REPO_ROOT=/Volumes/NVM2T/Development/autoloop)",
      "src/adapter/pi-rpc-adapter.mjs line 147: `piExecutable = \"pi\"` default + buildArgs() constructs `--mode rpc --no-session ...`",
      "test/pi-rpc-real-smoke.mjs: piExecutable 'pi', provider deepseek, model deepseek-v4-flash (the ONE authorized real-Pi smoke)",
      "scripts/deploy-rb-ssg-governor.sh: installs extensions to $HOME/.pi/agent/extensions — Pi's agent dir is ~/.pi/agent (getAgentDir())",
      "~/.pi/agent/auth.json + ~/.pi/agent/sessions exist: Pi's credential/session store",
      "npm registry: @earendil-works/pi-coding-agent bin = `pi`, current 0.84.2 (published 2026-08-14)"
    ],
    "not_a_protocol_regression": "P3's 'installed harness has no RPC server surface' finding is explained by the wrong package being inspected: @earendil-works/pi-ai is the LLM API library (no RPC server); the RPC server lives in the pi-coding-agent CLI, which was never installed.",
    "environment_vs_repository": {
      "ENVIRONMENT_FIX": "npm install -g @earendil-works/pi-coding-agent@0.84.2 (restores `pi` on PATH); provision deepseek credential into pi's own auth store (~/.pi/agent/auth.json) so the adapter's restricted child env (allowlist PATH/HOME/TMPDIR/LANG/LC_ALL/TERM, no API key) can authenticate — verified `pi auth check --provider deepseek --model deepseek-v4-flash` -> ready under that restricted env",
      "REPOSITORY_FIX": "none required — adapter JSONL RPC contract is still officially supported by the current protocol generation (verified empirically, see 02-protocol-compatibility-map.json)"
    }
  },
  "verification": {
    "pi_version": "0.84.2",
    "pia_launcher_version": "0.84.2 (pia --version)",
    "auth_check_restricted_env": "ready",
    "real_smoke": "PASS / AUTOLOOP_PI_RPC_SMOKE_OK_CONFIRMED (see 04-real-smoke/)"
  }
}
