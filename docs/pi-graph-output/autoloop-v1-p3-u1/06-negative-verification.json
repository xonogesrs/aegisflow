{
  "schema": "autoloop.p3-u1.negative-verification/v1",
  "card_id": "AUTOLOOP-V1-P3-U1-PI-EXECUTION-SEAM-RESTORE-1",
  "results": [
    {
      "id": "N1",
      "name": "Missing executor",
      "check": "execution must fail closed, no fallback to a fake runner",
      "method": "createPiRpcAdapter({piExecutable:'/nonexistent/pi-binary'}).runAdapter(...)",
      "observed": "status='error', error='spawn /nonexistent/pi-binary ENOENT', exitCode=null; no child, no fallback",
      "verdict": "PASS"
    },
    {
      "id": "N2",
      "name": "Protocol mismatch",
      "check": "wrong generation / unexpected event -> explicit incompatible failure",
      "method": "existing suite test/test-pi-rpc-adapter.mjs malformed_json / unknown-event scenarios (fake-pi-rpc fixture)",
      "observed": "adapter returns status='error' with protocol reason; unknown event types are recorded and ignored, never silently treated as completion",
      "verdict": "PASS (68/68 adapter+lifecycle tests)"
    },
    {
      "id": "N3",
      "name": "Timeout",
      "check": "real executor must support bounded termination",
      "method": "existing suite timeout scenarios (ignore-abort / descendant-child fixtures) + adapter's bounded terminateChild",
      "observed": "timed_out result with SIGKILL process-tree escalation; fake keep-alive process is killed, tests pass",
      "verdict": "PASS"
    },
    {
      "id": "N4",
      "name": "Nonzero process failure",
      "check": "must not be reported as PASS",
      "method": "existing suite process_disappeared / missing-terminal scenarios",
      "observed": "status='error' with reason='process_disappeared' (code/signal recorded), never completed",
      "verdict": "PASS"
    },
    {
      "id": "N5",
      "name": "Missing usage",
      "check": "execution can succeed without usage -> USAGE_PRESENT false / TOKEN_STATUS UNAVAILABLE, not an execution failure",
      "method": "adapter has no usage requirement in its contract; the scripted/fake suite completes without any usage field",
      "observed": "adapter result contains no usage field and does not fail when none is present; usage is out-of-contract for the adapter (original P3 phase wires it)",
      "verdict": "PASS"
    },
    {
      "id": "N6",
      "name": "Fake usage injection",
      "check": "smoke harness must not fabricate usage as provider result",
      "method": "inspection: fake-pi-rpc.mjs fixture emits no usage fields; the usage probe (scripts/pi-rpc-usage-probe.mjs) only records usage observed on the real wire (raw-protocol.jsonl is the verbatim child stdout); adapter never injects usage",
      "observed": "all usage samples in 05-usage-reachability/report.json come from the real pi process's protocol events",
      "verdict": "PASS"
    }
  ],
  "summary": "N1-N6 all PASS; no fake/deterministic substitute used anywhere in the PASS proof."
}
