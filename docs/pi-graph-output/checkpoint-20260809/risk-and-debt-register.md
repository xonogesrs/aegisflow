# Checkpoint 2026-08-09 — Risk and Debt Register

## Lifecycle authority

Closeout has two paths:

1. `runStateDrivenCloseout` reads persisted closeout state, applies metadata/identity/idempotency rules, then calls mandatory graph closeout.
2. `runMandatoryGraphCloseout` remains available for legacy in-memory callers.

`src/runtime/colima-graph-runner.mjs:467-497` selects state-driven closeout only when `closeout.statePath` is present. `scripts/gov-closeout-bundle.mjs` uses state-driven closeout. Seventeen historical `scripts/*-self-closeout.mjs` files still invoke mandatory closeout directly.

## Current truth

Current structured delivery says:

- card: `AUTOLOOP-TA3`
- bundle generated and validated: true
- external review required/attempted: true
- external status: `AWAITING_EXTERNAL_REVIEW`
- verdict: `null`
- bundle SHA-256: `3fc4a8e95b7bebcea7cf20e5a630af6310a12c78efeb0feeb9d145623891b285`

Current graph evidence and bundle report internal `PASS`. That is an internal graph/closeout result. It is not external acceptance. No contradiction exists if both labels stay qualified; unqualified “PASS” is a reporting risk.

Legacy integration gates still read harness-owned `external-review-result.json` and legacy `READY_FOR_REVIEW.txt` artifacts (`src/governance/integration-commit-gate.mjs:11-18`; `scripts/gov-commit-integration.mjs:69-82`). Current `delivery.json` is therefore not yet the sole verdict authority.

State-driven closeout also accepts a persisted graph evidence shape with limited schema/digest/provenance validation (`src/governance/closeout-state.mjs:189-232`) and derives verifier PASS from `graphResult.final`. Bespoke self-closeout scripts can supply synthetic all-PASS graph/review results. Treat internal PASS as untrusted until runner-owned, digest-bound evidence is required.

Verdict application copies bundle identity/SHA from the delivery record instead of re-reading and hashing the bundle (`scripts/gov-closeout-bundle.mjs:209-227`); forced delivery can set `reviewBundleValidated=false` (`scripts/gov-external-review-surface.mjs:141-172`), and `runStateDrivenCloseout` ignores the `writeCloseoutState` result (`src/governance/review-bundle.mjs:3337`). These are fail-closed authority defects.

## Risk register

| ID | Class | Finding | Evidence | Disposition |
|---|---|---|---|---|
| R-00 | CLOSED | Caller-supplied `scratchRoot` is recursively deleted after mount; no canonical run-root or symlink containment gate | `src/runtime/scratch-ownership.mjs` — `prepareOwnedScratchRoot` / `removeOwnedScratchRoot` / `assertOwnedScratchRoot` / `planOwnedScratchRoot` / `ownershipKey`; `src/runtime/colima-graph-runner.mjs` (`prepareOwnedScratchRoot` mount, `removeOwnedScratchRoot` cleanup); `src/v2/durable-graph.mjs` (`wipeScratchPreserving`, durable `artifacts/scratch-ownership.json` resume authority); `src/subagent/subagent-graph-runner.mjs` (`planOwnedScratchRoot`/`prepareOwnedScratchRoot`); `test/test-scratch-ownership.mjs` | CLOSED by `AUTOLOOP_R00_OWNED_SCRATCH_REPAIR_1` (implemented) and `AUTOLOOP_R00_OWNED_SCRATCH_REGISTER_RECONCILIATION_1` (2026-09-20, HEAD `84c7d16`, register-truth re-audit PASS). Historical disposition "derive/delete only run-scoped child" is IMPLEMENTED and independently re-audited against current HEAD. Closure evidence: callers supply only a scratch namespace; every mount/cleanup uses one deterministic run-scoped child derived from `ownershipKey(executionId, repoPath)` (32-hex) under a marker-bound `.autoloop-owned` parent; owner marker (`.autoloop-owner.json`, schema `autoloop.scratch-ownership/v1`) binds authorityDigest=sha256(authorityToken), uid, executionId, repoPath, namespacePath, ownedRoot, ownershipKey — all validated by `assertOwnedScratchRoot`; namespace/repo/worktree/root rejection (scratch may not be repository root/descendant, not a git boundary, not filesystem root); traversal fence (`..` segments rejected before resolution); symlink containment fence (existing and post-resolution symlink components rejected on namespace, owned root, marker namespace, and preserved paths); marker binding validation fails closed (schema, owner, authority digest, uid, path/namespace/execution/repository bindings); durable resume ownership artifact (`artifacts/scratch-ownership.json`, `autoloop.scratch-authority/v1`) written before the first resumable checkpoint so a crash cannot strand a resumable execution without scratch authority, and reconstructed on resume; `wipeScratchPreserving` is marker-anchored (`prepareOwnedScratchRoot` re-derivation + authority token required) and refuses preserved-path symlinks; production caller audit: `colima-graph-runner.mjs`, `durable-graph.mjs`, `subagent-graph-runner.mjs` — every cleanup path goes through `removeOwnedScratchRoot`/`wipeScratchPreserving` with derived paths and authority tokens; `RAW_BYPASS_REMAINING = NO`. Regression: `test-scratch-ownership` 6/6; live reproduction 26/26; adversarial planted-marker checks 6/6 (forged marker at derived key rejected on assert and delete, planted payload survives); governance 564/564; colima-all 79/79. |
| R-01 | CLOSED | Reviewer/repair/retry budget dimensions settle after lifecycle action; return values ignored; contract promises fail-closed pre-action behavior | `src/budget/enforcement.mjs` — `admitReviewer` / `confirmReviewerInvoked` / `recordReviewer` / `cancelReviewerReservation` / `recordRepair` / `recordRetry` / `recordConsumption` / `preDispatch` / `finalize` (NEG13); `src/budget/ledger.mjs` — `ledgerReserve` / `ledgerSettle` / `ledgerConfirm` / `assertOpKeyAvailable` / `resumeBudgetLedger`; `src/budget/contract.mjs` — `DIMENSION_METER_MAP` / `BUDGET_STATES`; `src/runtime/colima-graph-runner.mjs` — lifecycle hooks (`onPhaseStart` pre-dispatch gate, `onPhaseTerminal` settlement); `src/sop/proportional-sop.mjs` — direct-execution settlement chain; `src/lifecycle-runner.mjs` — TA-2 repair authority; `test/budget/` suites; `test/test-graph-closeout-integration.mjs` (R-01 seam test) | CLOSED by `AUTOLOOP_R01_BUDGET_LIFECYCLE_SETTLEMENT_LARGE_CLOSEOUT_1` (2026-09-20, base `fdbd95d`). Historical disposition "focused production-path gate test and bounded rework" is IMPLEMENTED. Audit findings: (1) reviewer dimension ALREADY_IMPLEMENTED — R3 atomic reservation (`admitReviewer` synchronous check+reserve, one invoke permission per logical attempt, replay rejected `BUDGET_RESERVATION_CONFLICT`), invocation confirmation at the real adapter seam, idempotent settlement, unresolved reservations fail closed at `finalize`; every return value enforced in both production runners (`BudgetHoldError`). (2) repair/retry are intentional B4 runtime METERS (`NEG10`: never merged with the TA-2 `admission.repair_budget` authority); repair gating lives in `lifecycle-runner.mjs` (`REPAIR_BUDGET_EXHAUSTED`); `ledgerConfirm` cannot fail on valid input, so no drift is reachable. (3) ledger semantics correct: conservative upper-bound reservation (§6), crash→resume settles in-flight at upper bound, receipt-log reconstruction proof, opKey duplicate fence, monotonic child merge, no underflow, no cross-dimension contamination, exhaustion fence blocks dispatch. (4) RESIDUAL repaired: `colima-graph-runner.mjs` `onPhaseTerminal` ignored the authoritative `recordConsumption` result — a refused/never-dispatched phase (e.g. budget pre-dispatch refusal) reached terminal without a reservation, the settlement failure was silently swallowed, and NEG13 later surfaced the drift as `BUDGET_RECONCILIATION_DIVERGED` instead of the authoritative budget hold code. Repair: `dispatchedPhases` set marks phases that passed the pre-dispatch gate; a never-dispatched phase's terminal is refusal evidence (`skipped:true`, no settlement — evidence and ledger stay consistent); a dispatched phase's failed settlement now throws `BudgetHoldError` with the ledger's own hold code (fail-closed, correct attribution). NEG13 remains the final consistency fence. Admission reproduction (14-case probe + phantom-settle simulation) closed; former misattribution reproduced pre-repair and is gone post-repair (seam test asserts the authoritative `BUDGET_EXHAUSTED` path with zero divergence). Regression: test/budget 86/86; governance 564/564; colima-all 79/79+1 (R-01 seam test); orchestrator 46/46; lifecycle+durable 42/42. |
| R-02 | CLOSED | Current TA3 report carries historical duplicate count and synthetic timing lineage while Current waits for external verdict | `src/governance/promotion-authority.mjs` — `readDeliveryEvidence` / `verifyDeliveryBundleDigest` (the ONLY acceptance inputs: Current `delivery.json` + `review-bundle.txt`, digest re-verified); `src/governance/review-bundle.mjs` — `writeGraphCloseoutEvidence` (per-executionId evidence snapshot), `deliverToExternalReviewSurface` (fresh trio per card, occupancy fail-closed), `rotateExternalReviewSurface` (R-14 collision-safe archive binding, Archive write-only), `currentSurfaceReviewStatus` (VCA1-F1 single structured read); `src/governance/closeout-state.mjs` — node evidence carries real `startedAt`/`completedAt` from graph results; `docs/pi-graph-output/vca1/vca1-r1-r2-evidence.json` — historical instrumentation explicitly classified REAL / SYNTHETIC / NOT_INSTRUMENTED with `reconstructionVerdict`; `test/governance/test-rld2-stale-delivery.mjs` (14 stale-fallback fences); `test/governance/test-external-review-surface.mjs` (10 surface lifecycle tests) | CLOSED by `AUTOLOOP_R02_CURRENT_EVIDENCE_PROVENANCE_LARGE_CLOSEOUT_1` (2026-09-20, base `8bda5ea`) — provenance audit verdict ALREADY_SAFE; no production mutation required. Findings: (1) the "17 near-duplicate scaffolds" were the 17 `scripts/*-self-closeout.mjs` one-shot wrappers, and the "synthetic timing lineage" was their hardcoded 5-min-step closeout node timings — the SOURCE was deleted by `AUTOLOOP_R04_WRAPPER_REMOVAL_IMPLEMENTATION_1` (2026-09-20; 0 self-closeout scripts remain), so no current path can mint synthetic timing. (2) The historical classification itself is explicitly labeled at the source (`historicalInstrumentation.SYNTHETIC` / `.REAL` / `.NOT_INSTRUMENTED` + `reconstructionVerdict`: "Historical per-gate wall time is NOT recoverable; no scope widening was performed to backfill") and remains stored unchanged — historically truthful. (3) The historical evidence has ZERO production consumers (`vca1`/`historicalInstrumentation`/`SYNTHETIC`: 0 hits in `src/`+`scripts/` production paths) — HISTORICAL_CONTEXT only, never VALIDATION or AUTHORITY input. (4) The TA3-era Current surface was rotated out long ago: Archive holds the TA2/TA3-era rotations (write-only; no authority reader exists), Current holds the current generation (PGMA1, PASS, digest-bound verdict, `supersedes:null`), and `deliverToExternalReviewSurface` builds each new trio fresh from THIS card's bundle + state + evidence — no inheritance from history by filename, ordering, or fallback (RLD NEG-1..NEG-10 fences: stale identity, stale generation, SHA mismatch, partial rotation, receipt mismatch all fail closed). (5) Acceptance authority (R-13 chain) reads ONLY Current `delivery.json` + `review-bundle.txt` with digest re-verification; `evidence.json` is delivered to the reviewer but is never an acceptance input; UNKNOWN provenance cannot enter the chain. (6) Current/evidence.json is regenerated per run by `writeGraphCloseoutEvidence` (executionId-named, atomic, secret-scanned) from the current graphResult under the R-12 GATE K validator. Regression: external-review-surface + stale-delivery 24/24; review-bundle + closeout-lifecycle + execution-review 59/59; promotion/integration gates 17/17; delivery+promotion-gate+invariant 67/67; governance 564/564; colima-all 80/80. |
| R-03 | P1 | Bare `pi` remains a wrong-cwd/repo-discovery route; launcher is opt-in and guidance is not OS enforcement | `scripts/pi-autoloop.sh`; VCA1 Phase 0b/0f evidence | Make canonical launcher operationally mandatory for AutoLoop work; retain guard limits |
| R-04 | CLOSED | Legacy closeout branch and 17 one-shot wrappers can mint parallel historical artifacts | `src/runtime/colima-graph-runner.mjs:467-497`; `scripts/*-self-closeout.mjs` | CLOSED by `AUTOLOOP_R04_WRAPPER_REMOVAL_IMPLEMENTATION_1` (2026-09-20, HEAD `1a562e0`): the legacy in-memory `closeout.requiresReview` branch in `colima-graph-runner.mjs` removed — closeout applicability is statePath-only via `runStateDrivenCloseout` (no fallback); all 17 `*-self-closeout.mjs` one-shot wrappers plus the 4 one-shot verdict resolvers (`ta2-resolve-ta1-verdict.mjs`, `ta2r-resolve-ta2-verdict.mjs`, `ta2r-resolve-ta2r-verdict.mjs`, `ta2r-resolve-ta2r-hold-verdict.mjs`) deleted (21 files, no tombstones). E2E closeout coverage migrated to the statePath seam (`test/test-graph-closeout-integration.mjs`); W1A S3/S10 + DE-2 production-caller floors reconciled to assert wrapper ABSENCE. Frozen forensic fixtures (`*-independent-review.mjs`, `*-verify.mjs`, `rld2-*` chain) retained per RC1 §10. Post-removal authority audit: production verdict authority exclusively through `applyExternalReviewVerdictForDelivery` (R-13 seam); 0 artifact-blind production callers remain (the sole low-level caller left is the sanctioned env-isolated forensic fixture `scripts/rld2-reproduction.mjs`). Regression: `test:governance` 564/564, `test:colima-all` 79/79 (fail 0, cancel 0), all targeted suites at admitted baselines. |
| R-05 | P2 | `wrapGraphHooks` duplicates runner wiring and is not called | `src/budget/graph-wiring.mjs:39-99`; call-site search | Merge/remove |
| R-06 | P2 | Telemetry observer is opt-in; default wall/token/tool/memory evidence is absent | `src/telemetry/graph-observer.mjs`; VCA1 evidence | Instrument only named authoritative stores; otherwise mark unknown |
| R-07 | P2 | Full Colima suite is expensive and historically killed without total timing | `package.json:test:colima-all`; VCA1 R1/R2 evidence | Scope-gate and retain forensic mode |
| R-08 | REMOVE candidate | Dormant Pi RPC/transport adapters have no production caller | `src/adapter/pi-rpc-adapter.mjs`; `src/v2/pi-transport-adapter.mjs`; probe/test-only references | Disable, prove no consumer, remove |
| R-09 | P1 | `runAutoLoop`, raw subagent, and direct script paths can bypass `runAdmittedGraph` | `src/autoloop.mjs:91-124`; raw `runSubagentGraph` callers | One canonical admitted production seam |
| R-10 | P1 | Memory retrieval executes when provider exists without checking projected `retrieval_allowed` | `src/admission/policy-projection.mjs:104-106`; `src/runtime/colima-graph-runner.mjs:201-218` | Add fail-closed retrieval authority |
| R-11 | CLOSED | Legacy commit/push/PR gates read `external-review-result.json`, not Current `delivery.json` | `scripts/gov-push-gate.mjs`; `scripts/gov-draft-pr.mjs`; `src/governance/integration-commit-gate.mjs`; `scripts/gov-commit-integration.mjs` | CLOSED. All three legacy authority seams migrated to the canonical chain (review-job ACCEPTED → delivery PASS bound to delivered-bundle digest → recomputed candidate identity → live bindings). Push/draft-PR gates: CLOSED by `AUTOLOOP-PGMA1` (implementation `54ea90a`, remote promotion anchor `governance/push-draft-gate-authority-migration` @ `7e04f4e75f1ddb261d4fa55167c655e5f54069d9`, closeout evidence `docs/pi-graph-output/pgma1/card-closeout-bundle-20260919-pgma1.txt`). Commit-integration gate: CLOSED by landing commit `b9f35f81813dd75c8f656c14d3765903f9af3d1c` on `governance/rsl2-universal-execution-review-surface` (Landing Admission + Landing Review PASS; retired artifact authority = NONE; reverse controls RC-A/RC-B/RC-C/RC-D green; governance regression 534/534). `external-review-result.json` is RETIRED / NON-AUTHORITY for push, draft PR, and commit integration; historical artifacts remain on disk as evidence only. |
| R-12 | CLOSED | State-driven closeout accepts graph evidence with weak schema/digest/provenance binding and can derive internal PASS from supplied result | `src/governance/closeout-state.mjs`; `src/governance/review-bundle.mjs` | CLOSED by `AUTOLOOP_R12_CLOSEOUT_ACCEPTANCE_*`. Technical landing commit `ea927e358dc3678c5d6b74709913f3c18a944cae` on `governance/rsl2-universal-execution-review-surface` (reviewed bytes frozen by `AUTOLOOP_R12_CLOSEOUT_ACCEPTANCE_REVIEW_AND_CLOSEOUT_1`, Review PASS): one canonical graph-evidence validator (`validateGraphEvidence` + `assertGraphAggregateConsistency` in `closeout-state.mjs`) serves both the in-memory graphResult path and the disk `autoloop.review-bundle.graph-closeout-evidence/v1` loader — exact schema enforcement, mandatory `task.cardId`/`graphRunId` identity with card/run binding against the closeout lineage, no silent `graph-unknown`/default-final synthesis, node normalization restricted to canonical runner shapes, required-node completeness from `scheduler.order`, aggregate/child contradiction fencing (`R12_AGGREGATE_CHILD_CONTRADICTION`), state non-authority preserved via durable `verifyAppliedCloseoutBundle` re-verification. Evidence: independent Review PASS (reverse controls RC-A/RC-B/RC-C/RC-D green; 20-case negative matrix fail-closed; disk/memory parity; real `runColimaGraph` E2E 2/2; CLI graph-evidence E2E PASS/HOLD/HOLD; governance regression 548/548, review-unit 17/17, promotion authority 27/27). R-13 (verdict application artifact binding) remains OPEN and out of scope for this closure. |
| R-13 | CLOSED | Verdict apply copies identity/SHA from delivery record; forced/unvalidated delivery remains possible; state write failure is ignored | `scripts/gov-closeout-bundle.mjs`; `src/governance/gov-external-review-surface.mjs`; `review-bundle.mjs` | CLOSED. RSL2-06 (`583acb9`, 2026-08-16) repaired the row's first two clauses: `--apply-verdict` re-reads the surface bundle and recomputes content SHA, `--force` delivery of unvalidated bundles removed, closeout-state disposition writes fail closed. Residual (self-consistent forged artifact + forged matching record could mint PASS; artifact-blind library seam; unfenced conflicting verdict) CLOSED by landing commit `a5b6da2ff848197c5bff1e2f4306e7962889d054` on `governance/rsl2-universal-execution-review-surface` (independent Review PASS, `AUTOLOOP_R13_VERDICT_ARTIFACT_BINDING_REVIEW_AND_CLOSEOUT_1`): artifact-aware `applyExternalReviewVerdictForDelivery` is the production verdict seam — single fresh read of the delivered artifact feeds structural validation (`validateReviewBundle`), content-SHA binding, bundle-identity extraction and CARD_ID binding against the delivery record's expected values (never proof); caller-supplied identity/SHA have zero authority; persisted verdict binds freshly verified values only; TOCTOU recheck guard fails closed on mid-verification mutation; conflicting verdict against a finalized receipt rejected (`EXTERNAL_REVIEW_VERDICT_CONFLICT`) with identical re-application idempotent; failed attempts leave the delivery record byte-unchanged. Former PASS-minting attack independently reproduced CLOSED. Evidence: 15-case tamper matrix fail-closed, RC-A–E green, real CLI + library E2E green, governance regression 559/559 (review-unit 17/17, promotion 27/27, runColimaGraph integration 2/2). Remaining one-shot `ta2r-resolve-*` scripts call the low-level transition with hardcoded historical identities — legacy card-closeout artifacts tracked under R-04 (REMOVE candidate), not a Current-surface authority path. |
| R-14 | CLOSED | Same-generation bundle bytes depend on time; peers retire after render; archive filenames can disagree with persisted status | `src/governance/review-bundle.mjs`; `scripts/gov-controller-prepare-round.mjs`; `scripts/gov-review-bundle.mjs` | CLOSED by landing commit `d44b35c579ae061982684adabaf43fb80278d409` on `governance/rsl2-universal-execution-review-surface` (independent Review PASS, `AUTOLOOP_R14_BUNDLE_BYTES_DETERMINISM_REVIEW_AND_CLOSEOUT_1`). Frozen invariant: same semantic generation + same authoritative inputs ⇒ identical canonical bytes ⇒ identical content SHA ⇒ identical `REVIEW_BUNDLE_IDENTITY` ⇒ identical canonical filename ⇒ unambiguous generation→artifact binding; archive resolution is digest-based, never filename-order. Repairs: (1) generation-bound `GENERATED_AT` — the closeout gate no longer injects wall-clock time; the renderer derives the timestamp from the card-start baseline `capturedAt` (then `closeout.appliedAt`; explicit caller pin still wins), so re-rendering one generation is byte/SHA/identity/filename deterministic; (2) collision-safe `rotateExternalReviewSurface` — every archived file carries a content-derived digest8 discriminator (`<date>-<card>-<identity8>-<verdict>-<digest8>-<name>`); identical re-rotation is idempotent, distinct bytes never overwrite (`archive_collision_non_identical` fail-closed), artifact/verdict/delivery pairing preserved; (3) digest-verified resolution — new `resolveAuthoritativeBundleByDigest` (identity alone never selects; exactly one self-consistent digest match accepted; zero → fail closed; >1 accepted only when byte-identical, else `ambiguous_representations` fail-closed), `verifyAppliedCloseoutBundle` rewired onto it, and both archive consumers (`gov-controller-prepare-round` prior-bundle derivation, `gov-review-bundle` prior-binding verification) require stated footer digest == recomputed content digest. Evidence: independent Review PASS (10-case negative matrix fail-closed: delayed rerender identical, stale earlier filename never wins, tampered/lying-footer excluded or ambiguous fail-closed, missing/wrong digest fail-closed, all archive collisions zero-overwrite, identical rerotation idempotent; R-13 non-regression green); governance regression 563/563 (review-bundle 30/30, external-review quad 79/79, delivery 27/27, execution-review 17/17, review-unit e2e 17/17, promotion 27/27, runColimaGraph integration 2/2). |

## Complexity red-team

Skeptical subtraction result:

- No value in keeping duplicate verification entries after W1A.
- No value in routine IR budget rerun when accounting can reuse recorded regression.
- No value in per-file accounting re-exec outside forensic diagnosis.
- No value in automatic full Colima matrix for non-runtime cards.
- No value in 17 card-specific closeout scripts after state-driven parity.
- No value in a second budget hook abstraction unless a production caller appears.
- No demonstrated need for Agent Plugins or Central Control Plane roadmap work.
- No demonstrated need for another policy/report nexus until current consumers and authorities are mapped.

## Lifecycle debt

State-driven closeout is the right authority seam, but migration is incomplete. Historical one-shot scripts, legacy in-memory closeout, and multiple output directories still exist. This is P2 cleanup unless a caller bypasses current delivery authority; current static evidence does not prove such bypass.

The preceding legacy gate findings upgrade part of this debt to P1: commit/push/PR authorization can consume a different verdict artifact than Current. Same-generation bundle identity also excludes `generatedAt` while rendered bytes include it, so retries can produce time-dependent bytes under one nominal generation. Freeze bytes/identity and retire bundle/evidence/delivery as one linked unit.

## P0 status

P0 established: unbounded recursive scratch deletion is reachable through `runColimaGraph`. P1 risks block a clean “fully enforced and externally accepted” claim. No repair authorized in this checkpoint.
